# Quarto build artifacts (keep freeze cache ignored; allow site outputs)
_site/
_freeze/

# Allow committing generated site (docs/) for GitHub Pages
# (GitHub Pages -> Settings -> Pages -> Build from /docs on main)


# Python
__pycache__/
*.pyc
.venv/

# EE creds (never commit real keys)
gee-key.json

# OS/editor cruft
.DS_Store
.vscode/

/.quarto/
**/*.quarto_ipynb

# Quarto intermediate files (just in case)
posts/**/*.md
# posts/**/*.ipynb
posts/**/*.knit.md
*_cache/
